# QUICK DEPLOYMENT - 3 COMMAND SETS

## Copy & Paste These Commands (3 Total)

### COMMAND SET 1: Git Setup (Run Once in PowerShell)
```powershell
cd C:\Users\HP\Desktop\security-baseline-checker
git config --global user.email "your-email@example.com"
git config --global user.name "Your Name"
```

### COMMAND SET 2: Push to GitHub (Replace YOUR_USERNAME)
```powershell
git remote add origin https://github.com/YOUR_USERNAME/security-baseline-checker.git
git branch -M main
git push -u origin main
```

### COMMAND SET 3: Future Updates (After Each Change)
```powershell
git add .
git commit -m "Your change description"
git push origin main
```

---

## Steps After Commands

1. **After Command Set 1:**
   - You've configured Git locally

2. **After Command Set 2:**
   - Go to your GitHub repo
   - Verify all files are there
   - Copy repo URL

3. **In Render Dashboard:**
   - Click "New" → "Web Service"
   - Connect repo
   - Fill in:
     - Name: `security-baseline-checker`
     - Start Command: `gunicorn app:app`
   - Click "Deploy"

4. **Wait 2-5 minutes** → Status changes to "Live"

5. **Copy your live URL** → Share with world! 🎉

---

## That's It!

You now have a production-ready app deployed to Render!
